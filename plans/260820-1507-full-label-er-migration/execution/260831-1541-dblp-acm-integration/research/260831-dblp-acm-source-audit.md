# Superseded DBLP-ACM source audit

> Superseded on 2026-09-01. The researcher rejected every observation from the
> prior remote audit as active contract evidence. Hashes, schemas/columns,
> counts, class balance, missingness, ranges, identity properties, duplicates,
> overlap, and derived schedules must be recalculated from a fresh local
> acquisition before use. The former values were removed to prevent accidental
> reuse.

Candidate references retained only to bootstrap the new acquisition:

- DeepMatcher datasets index: <https://github.com/anhaidgroup/deepmatcher/blob/master/Datasets.md>
- Candidate structured source root: <https://pages.cs.wisc.edu/~anhai/data1/deepmatcher_data/Structured/DBLP-ACM/exp_data/>
- Candidate filenames: `tableA.csv`, `tableB.csv`, `train.csv`, `valid.csv`,
  `test.csv`

The fresh local inspection has replaced this note as scientific evidence:
`configs/datasets/observations/dblp_acm_2018_06_29_a15b752f.json`.
